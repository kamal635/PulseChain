import 'package:flutter/widgets.dart';

/// Defines the color palette for the application.
abstract final class AppColors {
  /// The brand accent color.
  static const Color brandAccentGreen = Color(0xFF31FFA9);

  /// The background color.
  static const Color background = Color(0xFFF8FAFC);

  /// The error red color.
  static const Color errorRed = Color(0xFFFF4D4D);

  /// The brand primary color.
  static const Color brandPrimary = Color(0xFF0051FD);

  /// A low emphasis surface color.
  static const Color surfaceLow = Color(0xFFF3F4F6);

  /// The card stroke color.
  static const Color cardStroke = Color(0xFFE2E8F0);

  /// The Dark Gray color.
  static const Color darkGray = Color(0xFF444444);

  /// The black color.
  static const Color black = Color(0xFF000000);

  /// The white color.
  static const Color white = Color(0xFFFFFFFF);
}
